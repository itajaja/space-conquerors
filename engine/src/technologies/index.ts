import { ITechnology, ITechnologyFamily } from '../definitions'

const technologies: { [idx: string]: ITechnology } = {}
export const technologyFamilies: { [idx: string]: ITechnologyFamily } = {}

export default technologies
